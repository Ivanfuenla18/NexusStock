{
  "name": "nexusstock",
  "version": "1.0.0",
  "description": "Sistema de gestión de inventario Fullstack",
  "main": "server.js",
  "scripts": {
    "server": "npm run dev --prefix Server",
    "update": "git add . && git commit -m \"NexusUpdate\" && git push origin main",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ivan",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}
